name: Document Watcher CI

on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Show Current Directory
        run: pwd

      - name: List Repository Files
        run: ls -la

      - name: Validate Bash Syntax
        run: bash -n scripts/watcher.sh
